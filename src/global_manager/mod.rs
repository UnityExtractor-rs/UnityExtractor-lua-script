
pub struct GlobalManager{
    /// a manager to create / remove / edit the preview object for script
    script_obj:(),
    /// fs manager
    /// a manager interactive with file system
    /// 1. require open / save to file
    /// 2. require a dir to output 
    fs:()
}